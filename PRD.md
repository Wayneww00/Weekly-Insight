# Insight Hub PRD

## 1. Executive Summary

### Problem Statement

团队每周产出行业洞察 PPT/PDF 后，通常通过群聊发送给老板和相关同事。但群聊消息流量大，文件容易被后续聊天淹没，导致老板错过、回看困难、历史资料无法沉淀和检索。

### Proposed Solution

建设一个轻量级的 Insight Hub：同事上传 Weekly Insight 或 Monthly Insight 的 PPT、PPTX、PDF 后，系统自动生成可在线浏览的 HTML 版本，并按期数归档。老板可以通过一个固定链接查看最新一期，也可以按时间、主题、行业、关键词查看历史洞察。

### Success Criteria

- 最新一期从上传完成到可在线浏览的时间 <= 3 分钟。
- 老板或管理层通过固定入口找到最新一期的成功率 >= 95%。
- 历史内容按关键词搜索返回结果时间 <= 1 秒，支持至少 2 年历史文件。
- 上传者完成一次发布所需操作 <= 3 步。
- 文件转 HTML 成功率 >= 98%，失败时必须保留原文件下载和明确错误提示。

### Default Assumptions

- 使用对象主要是内部团队，不面向公众开放。
- 首期优先解决“上传、在线查看、历史归档、固定入口”问题。
- PPT/PDF 的视觉还原度比复杂在线编辑能力更重要。
- 老板主要在手机、平板、电脑浏览器中查看。

## 2. User Experience & Functionality

### User Personas

**内容上传者**

负责每周制作并发布行业洞察文件，关注上传是否简单、发布是否可靠、内容是否能被老板及时看到。

**老板/管理层**

每周查看最新行业洞察，偶尔回看历史内容，关注入口是否固定、打开是否快、手机阅读是否舒服。

**团队成员**

需要查找某一期历史洞察，用于复盘、准备会议、引用观点或补充研究。

**系统管理员**

负责权限、分类、存储容量、失败文件处理和内容维护。

### Core User Stories

**Story 1: 上传并发布周报**

As a 内容上传者, I want to 上传 PPT/PPTX/PDF 并自动生成网页版本, so that 老板可以通过固定链接查看最新内容。

Acceptance Criteria:

- 支持上传 `.ppt`, `.pptx`, `.pdf` 文件。
- 上传时必须选择洞察类型（Weekly Insight 或 Monthly Insight）、所属日期、行业分类，并可选填写摘要。
- 系统根据所属日期和洞察类型自动生成标题，例如 `2026-05-25 Weekly Insights`。
- 上传完成后系统自动生成在线浏览页面。
- 生成完成后，该文件可被设为“最新一期”。
- 上传失败或转换失败时，系统显示明确原因，并保留原文件重新处理入口。

**Story 2: 查看最新一期**

As a 老板, I want to 打开固定链接直接看到最新一期洞察, so that 我不需要在群聊里翻文件。

Acceptance Criteria:

- 首页首屏展示最新一期标题、日期、摘要和“开始阅读”入口。
- 最新一期阅读页支持逐页浏览、缩略图导航、全屏查看和原文件下载。
- 移动端阅读体验可用，不出现文字和页面控件重叠。
- 页面加载失败时提供原 PDF/PPT 下载兜底。

**Story 3: 查看历史归档**

As a 团队成员, I want to 按时间和主题查找历史洞察, so that 我可以快速找到过去某一期资料。

Acceptance Criteria:

- 提供历史列表，默认按发布时间倒序排列。
- 支持按年份、月份、行业分类筛选。
- 每一期展示标题、日期、上传者、分类、摘要和文件类型。
- 支持打开在线 HTML 版本，也支持下载原文件。

**Story 4: 搜索内容**

As a 使用者, I want to 搜索标题、摘要和正文关键词, so that 我不必记得文件发布时间。

Acceptance Criteria:

- 支持搜索标题、摘要、标签和从文件中提取的文本。
- 搜索结果展示命中关键词、所属期数和页面入口。
- 搜索 2 年以内历史内容时，响应时间 <= 1 秒。

**Story 5: 管理内容与权限**

As a 系统管理员, I want to 管理文件、分类和访问权限, so that 内容安全且归档长期可维护。

Acceptance Criteria:

- 只有授权用户可以上传、编辑、删除或设为最新一期。
- 普通访问者只能查看已发布内容。
- 删除操作需要二次确认，且建议采用软删除。
- 重要操作记录审计日志，包括上传、发布、删除、权限变更。

### Key Experience Principles

- **固定入口优先**：老板永远只需要记住一个链接。
- **最新内容优先**：首页默认突出最新一期，而不是复杂菜单。
- **历史沉淀清晰**：所有上传都按时间归档，避免文件散落在聊天记录里。
- **转换失败可兜底**：HTML 转换失败时，至少可以在线预览 PDF 或下载原文件。
- **上传者低负担**：上传流程尽量接近“选文件、选类型和日期、发布”，标题由系统自动生成。

### Non-Goals

- MVP 不做在线 PPT 编辑器。
- MVP 不做复杂审批流。
- MVP 不做评论区和群聊替代功能。
- MVP 不做对外公开分享和营销官网。
- MVP 不强制实现 AI 自动写洞察，但可为后续版本预留摘要和标签能力。

## 3. AI System Requirements

AI 功能不是 MVP 必需项，但建议作为 v1.1 增强能力。

### Candidate AI Features

- 自动提取 PPT/PDF 摘要、关键词和行业标签。
- 自动生成每期 3-5 条 executive summary。
- 从历史内容中进行语义搜索，例如“最近有哪些关于新能源出口的观点”。
- 自动识别图片页和数据图页，为搜索结果生成页面级引用。

### Tool Requirements

- 文档解析：提取 PPT/PDF 中的文本、图片和页面结构。
- OCR：处理扫描版 PDF 或图片型页面。
- Embedding/向量检索：支持语义搜索历史洞察。
- LLM 摘要：生成可编辑的摘要、标签和重点观点。

### Evaluation Strategy

- 摘要可用率：上传者无需大改即可采用的摘要比例 >= 80%。
- 标签准确率：人工抽样评估，Top 3 标签准确率 >= 85%。
- 搜索命中率：用 30-50 个历史问题构建测试集，Top 5 结果命中率 >= 85%。
- 引用准确性：AI 回答必须能定位到具体期数或页面，引用准确率 >= 95%。

## 4. Technical Specifications

### Architecture Overview

系统由 6 个核心模块组成：

1. **Upload Service**：处理文件上传、格式校验、元数据填写和存储。
2. **Conversion Service**：将 PPT/PPTX/PDF 转换为可在线浏览的 HTML 或图片/PDF 预览资源。
3. **Content Repository**：保存原文件、转换产物、封面图、文本索引和元数据。
4. **Reader Web App**：提供首页、最新一期、历史归档、搜索、阅读器页面。
5. **Admin Console**：提供上传、发布、编辑、删除、重试转换、权限管理。
6. **Search Index**：索引标题、摘要、标签、正文文本，后续可扩展向量检索。

### Recommended Conversion Strategy

MVP 建议采用“页面级渲染 + HTML 阅读器”的方式，而不是完全还原 PPT 的 DOM。

具体方案：

- PPT/PPTX 先转换为 PDF。
- PDF 每页渲染为高清图片或 Canvas 可浏览页面。
- 同步提取文本用于搜索。
- 前端阅读器以“页”为单位展示，支持缩略图、翻页、全屏和下载原文件。

这样可以最大化视觉还原度，降低不同 PPT 模板、字体、动画、图表导致的兼容性风险。

### Data Model

**InsightIssue**

- `id`: 唯一 ID
- `title`: 系统生成标题；Weekly 使用 `{YYYY-MM-DD} Weekly Insights`，Monthly 使用 `{YYYY-MM} Monthly Insights`
- `insightType`: weekly / monthly
- `summary`: 摘要
- `issueDate`: 所属日期
- `publishedAt`: 发布时间
- `status`: draft / processing / published / failed / archived
- `category`: 行业分类
- `tags`: 标签列表
- `uploaderId`: 上传者
- `originalFileId`: 原始文件
- `convertedAssetId`: 转换产物
- `coverImageUrl`: 封面图
- `isLatest`: 是否为最新一期

**InsightAsset**

- `id`: 唯一 ID
- `issueId`: 所属期数
- `type`: original / pdf / pageImage / html / text
- `url`: 存储地址
- `pageNumber`: 页码，可为空
- `mimeType`: 文件类型
- `size`: 文件大小

**SearchDocument**

- `issueId`: 所属期数
- `pageNumber`: 页码
- `title`: 系统生成标题
- `content`: 提取文本
- `tags`: 标签
- `category`: 分类

### Core User Flow

**上传发布流程**

1. 上传者进入管理后台。
2. 点击上传，选择 PPT/PPTX/PDF。
3. 选择洞察类型（Weekly Insight 或 Monthly Insight）、日期和分类，可选填写摘要。
4. 系统根据日期和洞察类型自动生成标题，例如 `2026-05-25 Weekly Insights`。
5. 系统校验文件格式和大小。
6. 系统创建记录，状态为 `processing`。
7. 转换服务生成 PDF/页面图/文本索引/封面。
8. 上传者预览转换结果。
9. 上传者点击发布，内容出现在首页和历史列表。

**老板查看流程**

1. 老板打开固定链接。
2. 首页自动展示最新一期。
3. 点击开始阅读。
4. 在阅读器中逐页查看、全屏查看或下载原文件。
5. 如需回看，进入历史归档或搜索。

### Integration Points

- 文件存储：对象存储或本地文件系统，生产建议对象存储。
- 数据库：关系型数据库存储期数、用户、权限、资产元数据。
- 搜索：MVP 可用数据库全文索引，后续升级为 OpenSearch/Meilisearch/向量数据库。
- 认证：内部 SSO、企业微信/飞书登录、或账号密码登录。
- 通知：发布成功后可自动发送群消息或邮件，消息中只包含固定链接和本期标题。

### Security & Privacy

- 所有页面默认需要登录访问。
- 原文件和转换产物不得使用永久公开 URL。
- 文件下载链接应支持过期签名。
- 上传文件需限制格式和大小，建议 MVP 单文件 <= 200MB。
- 后台操作需按角色授权。
- 审计日志至少保存 1 年。
- 删除内容应默认软删除，避免误删历史材料。

### Performance Requirements

- 首页首屏加载时间 <= 2 秒。
- 阅读器打开首屏页面 <= 3 秒。
- 单文件转换时间目标 <= 3 分钟，超过 5 分钟显示处理中状态并允许稍后查看。
- 支持至少 500 期历史内容和每期 100 页以内文件。
- 搜索响应时间 <= 1 秒。

### Error Handling

- 文件格式不支持：上传前端直接提示支持格式。
- 文件过大：提示大小限制，并建议压缩或拆分。
- 转换失败：记录失败原因，允许管理员重试。
- 字体缺失或版式偏移：展示转换预览，由上传者确认是否发布。
- 搜索索引失败：不影响内容发布，但后台提示索引异常并允许重建。
- 阅读器加载失败：提供原文件下载兜底。

## 5. MVP Scope

### Must Have

- 登录访问。
- 上传 PPT/PPTX/PDF。
- 选择洞察类型、日期、分类，可选填写摘要。
- 系统自动生成标题，例如 `2026-05-25 Weekly Insights` 或 `2026-05 Monthly Insights`。
- 自动生成在线阅读版本。
- 首页展示最新一期。
- 历史归档列表。
- 按标题、摘要、正文文本搜索。
- 原文件下载。
- 后台重试转换。

### Should Have

- 缩略图页导航。
- 设置某一期为最新一期。
- 发布成功后自动通知群聊。
- 按行业分类和年份筛选。
- 软删除和审计日志。

### Could Have

- AI 自动摘要。
- AI 自动标签。
- 语义搜索。
- 阅读数据统计，例如浏览次数、最近查看时间。
- 收藏或置顶重点期数。

### Won't Have in MVP

- 在线编辑 PPT。
- 多人实时协作。
- 评论讨论。
- 复杂审批。
- 外部客户访问。

## 6. Roadmap

### MVP: 固定入口与历史归档

目标：解决“文件被群聊淹没”的核心问题。

范围：

- 上传、转换、发布、查看最新一期、历史归档、基础搜索、下载。

### v1.1: AI 辅助整理

目标：降低上传者整理成本，提高历史搜索效率。

范围：

- AI 摘要、标签推荐、页面级文本提取、语义搜索、相关内容推荐。

### v1.2: 组织协作与治理

目标：提升长期运营能力。

范围：

- 权限分组、审计日志增强、阅读数据分析、内容质量检查、自动通知。

### v2.0: Insight Knowledge Base

目标：从“文件归档”升级为“行业洞察知识库”。

范围：

- 跨期趋势分析、主题专题页、AI 问答、引用来源、重点观点沉淀。

## 7. Metrics & Analytics

### Product Metrics

- 每周上传成功率。
- 转换成功率和平均转换耗时。
- 最新一期浏览人数。
- 老板打开最新一期的平均时间。
- 历史搜索次数。
- 搜索无结果率。
- 原文件下载次数。

### Operational Metrics

- 转换失败原因 Top 5。
- 平均文件大小。
- 每期平均页数。
- 存储占用增长。
- 权限异常或访问失败次数。

## 8. Risks & Mitigation

### Risk 1: PPT 转换还原度不稳定

Mitigation:

- MVP 使用 PDF/图片页渲染路线，优先保证视觉一致。
- 发布前提供预览确认。
- 保留原文件下载兜底。

### Risk 2: 上传者不愿意多填信息

Mitigation:

- 必填字段只保留洞察类型、日期、分类。
- 摘要和标签可选，后续用 AI 自动生成。
- 标题由系统根据日期和洞察类型自动生成。

### Risk 3: 老板还是习惯看群消息

Mitigation:

- 每次发布后自动向群聊推送固定链接。
- 群消息只发链接，不再发散落文件。
- 首页永远展示最新一期，降低点击后的寻找成本。

### Risk 4: 历史内容越多越难找

Mitigation:

- MVP 做基础全文搜索和分类筛选。
- v1.1 引入 AI 标签和语义搜索。
- 长期构建专题和趋势页。

### Risk 5: 内部资料泄露

Mitigation:

- 默认登录访问。
- 下载链接短期有效。
- 按角色控制上传、删除和查看权限。
- 保留审计日志。

## 9. Open Questions

1. 访问权限是仅公司内网、SSO 登录，还是需要支持外部临时访问？
2. 老板主要使用手机查看还是电脑查看？这会影响阅读器默认布局。
3. 是否需要保留 PPT 动画？如果不需要，PDF/图片页渲染是更稳的方案。
4. 每周文件大概多少页、多少 MB？
5. 是否需要自动同步到企业微信、飞书或邮件通知？
6. 历史文件是否已有一批需要批量导入？

## 10. Recommended MVP Definition

我建议第一版聚焦为一句话：

> 让团队每周上传一次文件，老板永远通过同一个链接查看最新一期，并能随时搜索和回看历史。

第一版不要做复杂知识库、AI 问答或在线编辑。先把“固定入口 + 在线阅读 + 历史归档 + 基础搜索”做扎实，产品价值会非常直接，推动团队使用也更容易。
